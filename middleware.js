export {default} from "next-auth/middleware"

export const config = {
    matcher: [
        "/home",
        "/parcelas",
        "/plot",
        "/create",
        "/transfer",
        "/update",
        "/users",
        "/anomalies",
        "/mate",
        "/ger"
    ]
}
